info:
  name: GetProductWithAuth
  type: http

http:
  method: GET
  url: "{{baseUrl}}/auth/product/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​