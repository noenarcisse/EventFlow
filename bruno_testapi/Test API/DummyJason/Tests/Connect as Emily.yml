info:
  name: Connect as Emily
  type: http
  seq: 1

http:
  method: POST
  url: https://dummyjson.com/auth/login
  body:
    type: json
    data: |-
      {
         "username": "emilys",
          "password": "emilyspass"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
